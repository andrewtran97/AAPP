name: ci

permissions:
  contents: read

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  basic:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show repository state
        run: |
          git status
          git log -1 --pretty=fuller

      - name: Block Claude/Anthropic attribution
        run: |
          if git log --all --format='%H%n%an <%ae>%n%B%n---' \
            | grep -Eiq 'Claude|Anthropic|Generated with Claude Code|Co-Authored-By:.*Claude|noreply@anthropic\.com'
          then
            echo "FAIL: Claude/Anthropic attribution found."
            exit 1
          else
            echo "OK: no Claude/Anthropic attribution found."
          fi

      - name: Basic file check
        run: |
          test -f README.md
          test -f spec/agent-action-record.schema.json
          test -f spec/aapp-scope.schema.json
          python3 -m json.tool spec/agent-action-record.schema.json >/dev/null
          python3 -m json.tool spec/aapp-scope.schema.json >/dev/null
          python3 -m json.tool examples/simple-tool-call/scope.demo.json >/dev/null
          python3 -m json.tool examples/simple-tool-call/scope.blocked.json >/dev/null
          echo "OK: required files exist"

      - name: Run unit tests
        run: |
          python3 -m unittest discover -s tests -v

      - name: Run AAPP demo
        run: |
          python3 -m aapp.cli scope-check --scope examples/simple-tool-call/scope.demo.json --actor-type agent --tool-type file_read
          python3 -m aapp.cli demo --scope examples/simple-tool-call/scope.demo.json --out evidence/demo
          python3 -m aapp.cli verify evidence/demo/trace.jsonl --key-file evidence/demo/dev.key
          python3 -m aapp.cli report evidence/demo/trace.jsonl --out evidence/demo/evidence.report.md

      - name: Block unauthorized scope
        run: |
          if python3 -m aapp.cli demo --scope examples/simple-tool-call/scope.blocked.json --out evidence/blocked-demo
          then
            echo "FAIL: blocked scope unexpectedly allowed recording"
            exit 1
          else
            echo "OK: blocked scope rejected"
          fi
